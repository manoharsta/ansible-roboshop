- name: installing nginx server and setting up web frontend
  hosts: web
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.yum:
      name: nginx
      state: installed
  
  - name: Start and enable nginx service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes